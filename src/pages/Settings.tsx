import { Box } from "@mui/material";
import Lists from "../components/settings/List";

export const Settings = () => {
  return (
    <Box component="main" sx={{ flexGrow: 1, p: 3 }}>
      <Lists />
    </Box>
  );
};
